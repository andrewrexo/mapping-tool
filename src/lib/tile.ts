const tileWidth = 64;
const tileHeight = 32;

// frame rate for animated tiles
const animationFrameRate = 5;

// tiles with wileWidth * animationFrameCount will be treated as an animated tile
const animationFrameCount = 4;

export default {
  tileWidth,
  tileHeight,
  animationFrameRate,
  animationFrameCount
};
